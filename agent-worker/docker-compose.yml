version: '3.8'

services:
  agent-worker:
    build: .
    container_name: agent-worker
    env_file:
      - .env
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    command: uvicorn worker.api:app --host 0.0.0.0 --port 8001 --reload