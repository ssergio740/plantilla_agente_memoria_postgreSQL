version: '3.8'

services:
  api:
    build: .
    container_name: webhook_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Habilitar reload para desarrollo
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
